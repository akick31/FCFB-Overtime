import RegistrationForm from '../auth/RegistrationForm';

const Register = () => {
    return (
        <div className="Register">
            <RegistrationForm />
        </div>
    );
}

export default Register;