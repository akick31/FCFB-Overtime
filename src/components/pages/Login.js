import LoginForm from '../forms/LoginForm';
import useFetch from '../hooks/useFetch';

const Login = () => {
    return (
        <div className="Login">
            <LoginForm />
        </div>
    );
}

export default Login;